import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const OrganizerListScreen = props => {

  return (
    <View>
      <Text>OrganizerListScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  
});

export default OrganizerListScreen;