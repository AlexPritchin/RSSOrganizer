import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const RSSListScreen = props => {
    return (
        <View>
            <Text>RSSListScreen</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default RSSListScreen;